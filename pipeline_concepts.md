# TileLang `T.Pipelined` 相关概念整理

这份文档整理了我们围绕 TileLang `T.Pipelined`、软件流水、`stage`、`num_stages`、流水深度、多缓冲等概念的讨论。

这里**刻意不包含**之前讨论过的一个特殊问题：FlashAttention 中 online softmax 那类“后一轮依赖前一轮状态更新”的跨轮递推问题。

本文只聚焦于 `T.Pipelined` 本身相关的通用概念与理解框架。

## 1. `T.Pipelined` 是什么

在这个项目里，用户侧 DSL 入口是 `T.Pipelined(...)`。

它本身并不是“直接发出某个硬件流水指令”，而是：

- 把一个循环标记为“希望做软件流水分析和改写”
- 让编译器去分析循环体中的数据依赖
- 让编译器为循环体中的语句分配流水位置
- 让编译器在需要时引入多缓冲
- 最终把原始循环改写成流水化后的形式

所以它更准确地表示：

**这个循环要被编译器当作一个可流水化的循环来处理。**

## 2. 一个最简单的例子

例如下面这个 GEMM 风格的循环：

```python
for k in T.Pipelined(K_tiles, num_stages=2):
    T.copy(A[k], A_shared)
    T.copy(B[k], B_shared)
    T.gemm(A_shared, B_shared, C_local)
```

如果不做流水，它的直观执行方式是：

```text
第 k 轮：
1. 搬运 tile k
2. 计算 tile k
```

如果做流水，目标通常会变成：

```text
时刻 t:     搬运 k
时刻 t + 1: 计算 k，同时搬运 k + 1
时刻 t + 2: 计算 k + 1，同时搬运 k + 2
```

也就是说：

- 某一轮负责“准备数据”
- 另一轮负责“消费前面准备好的数据”
- 不同逻辑迭代在流水线上错开流动

## 3. 逻辑迭代

**逻辑迭代**指的是原始循环里的一次迭代，也就是流水化之前 `for k in ...` 的一轮。

例如：

```python
for k in range(K_tiles):
    ...
```

那么：

- `k = 0` 是一个逻辑迭代
- `k = 1` 是一个逻辑迭代
- `k = 2` 是一个逻辑迭代

每个逻辑迭代通常处理原始大问题中的一个 tile。

对于 GEMM，这个 tile 一般由 `block_k`、`block_n` 等 blocking 参数决定。

## 4. 循环次数

**循环次数**就是原始逻辑迭代的总数。

例如：

```python
for k in range(K_tiles):
```

这里循环次数就是 `K_tiles`。

无论后面是否做流水，这个逻辑迭代数本身是不变的。

## 5. 时间片 / 时间单元 / 拍

为了描述流水，需要引入一个时间轴。

我们讨论里用过这些词：

- 时间片
- 时间单元
- 拍

它们可以基本按同义词理解。

一个实用定义是：

**一拍 = 流水线整体向前推进一步。**

### 串行时

在完全串行的情况下，通常：

- 一轮原始循环大致可以看作一拍

### 流水后

在流水化之后：

- 一拍里可能包含多个不同逻辑迭代的不同操作

例如：

```text
拍 t:     copy(k)
拍 t + 1: gemm(k) + copy(k + 1)
```

所以在流水之后：

- “一拍”不再等于“一轮完整原始循环”
- 一拍里可以同时出现不同迭代、不同阶段的操作

## 6. `stage` 是什么

`stage` 表示：

**某条操作在流水线中的相对位置。**

它不是“流水深度”本身，而更像是：

- 这条操作属于流水线中的第几段
- 或者说，这条操作相对该逻辑迭代的起点，要延后多少拍执行

还是看简单 GEMM 例子：

```python
for k in range(K_tiles):
    copy A[k] -> A_shared
    copy B[k] -> B_shared
    gemm(A_shared, B_shared, C_local)
```

最自然的理解是：

- `copy A` 和 `copy B` 属于较早的阶段
- `gemm` 属于较晚的阶段

如果简单地说：

- copy 在 `stage 0`
- gemm 在 `stage 1`

那么意思就是：

- tile `k` 在拍 `t` 被搬进来
- tile `k` 在拍 `t + 1` 被用于 GEMM

所以 `stage` 的核心意思是：

**这个操作在流水线时间轴上的相对位置。**

## 7. 简单 GEMM 里有几个 stage

对于：

```python
for k in range(K_tiles):
    copy A[k] -> A_shared
    copy B[k] -> B_shared
    gemm(A_shared, B_shared, C_local)
```

最自然的逻辑划分是：

- 数据准备阶段
- 计算阶段

所以从概念上看，它通常可以理解成 **2 个逻辑 stage**：

- stage 0：准备数据
- stage 1：使用数据计算

这里要注意一个容易混的点：

- 循环体里虽然有 3 条语句
- 但不一定就有 3 个 stage

很多时候：

- `copy A`
- `copy B`

都可以合并看成一个“生产数据”的阶段；

而：

- `gemm`

可以看成一个“消费数据”的阶段。

当然，在更复杂场景里，编译器也可能把这些再细分。

## 8. 生产者 / 消费者视角

理解 `stage` 最简单的方法之一，就是用生产者-消费者关系去看。

在这个简单 GEMM 里：

- copy 负责产生 `A_shared` 和 `B_shared`
- gemm 负责消费 `A_shared` 和 `B_shared`

所以可以抽象成：

```text
生产者阶段 -> 消费者阶段
```

这也是为什么在简单矩阵乘主循环里，用“两段流水”来理解通常最清楚。

## 9. `num_stages` 是什么

`num_stages` 和 `stage` 有关，但不能简单把它们当成同一个东西。

更稳妥的理解是：

- `num_stages` 表示流水允许拉开的深度
- 它通常和多版本缓冲的轮转深度有关
- 它决定了流水中最多允许多少轮逻辑迭代以错开的方式共存

所以它更接近：

**流水线深度预算 / 多缓冲轮转深度。**

在很多简单例子里，`num_stages` 看起来会和“最大 stage 偏移 + 1”接近，但不建议把它机械记成一个死公式。

## 10. 流水深度

**流水深度**表示：

**同一时刻，流水线上最多有多少个不同逻辑迭代处于活跃状态。**

例如如果某一拍中，系统里同时活跃着：

- `k`
- `k - 1`
- `k - 2`

这三轮逻辑迭代，那么就可以说流水深度是 3。

这和“一个循环里有几个 stage”不是同一个概念。

## 11. 为什么 stage 数量和流水深度不是一回事

这是最容易混的地方之一。

对简单 GEMM：

- 逻辑上可以只有 2 个 stage
- 但流水深度可以是 2，也可以是 3

意思是：

- 一个逻辑迭代内部，只分成“准备”和“计算”两段
- 但流水线上可以同时挂着 2 轮、3 轮甚至更多不同迭代

所以：

- `stage` 数量描述的是“一轮内部的结构”
- 流水深度描述的是“时间轴上同时活着多少轮”

## 12. 并发度

**并发度**和流水深度有关，但也不是同一个东西。

一个很实用的区分方式是：

- 流水深度是逻辑上的“挂了多少轮”
- 并发度是硬件上“真实有多少工作能同时跑”

例如：

- 逻辑上流水深度是 3
- 但硬件只能支持 `1 copy + 1 compute`

那么：

- 流水中仍然可能同时挂着 3 轮数据
- 但在真实吞吐上看到的重叠，可能更接近两类活动的并行

所以：

- 流水深度大，不等于真实并发就一定同比增大
- 真实并发还受执行单元数量、带宽、同步、依赖约束

## 13. 数据依赖

流水能不能合法排起来，最核心的限制就是数据依赖。

最简单的理解方式：

- 只有不破坏先后依赖的操作才能重叠
- 只有数据在逻辑上已经准备好了，后面的消费者才能提前错开去执行

对于简单 GEMM：

- copy 必须先于 gemm 使用该 tile

所以合法流水通常是：

```text
copy(k) 在 gemm(k) 之前
copy(k + 1) 可以和 gemm(k) 重叠
```

依赖关系决定了：

- 哪些语句可以挪动
- 哪些语句不能挪动

## 14. 多缓冲（multi-buffer）

多缓冲是流水化成立时最重要的空间机制之一。

为什么需要它？

因为在流水里，常常会出现：

- 一轮还在读旧数据
- 下一轮已经想写新数据

如果它们共用同一个 buffer，就会出现覆盖冲突。

所以必须准备多个 buffer 槽位，让不同逻辑迭代的数据同时存在。

例如双缓冲时：

- 第 `k` 轮 gemm 读 `buf0`
- 第 `k + 1` 轮 copy 写 `buf1`

下一拍再轮换。

所以多缓冲本质上是：

**用多个存储槽位承接不同在途逻辑迭代的数据，避免读写互相覆盖。**

## 15. 多 buffer 数量

多 buffer 数量和流水深度高度相关，但不建议机械地认为它们总是严格一一相等。

更稳妥的理解是：

- 有多少轮数据会同时在系统里活着
- 就通常需要相应数量的 buffer 槽位来安全承接

在很多简单软件流水里：

- `num_stages`
- 流水深度
- buffer 版本数

常常非常接近，甚至在实现里相同；

但从概念上，最好还是分开理解：

- 多 buffer 数量回答的是“空间上准备了几个槽位”
- 流水深度回答的是“时间上同时挂了几轮”

## 16. 核内视角

在单个核内部，多缓冲最经典的用途就是：

- 当前一轮在计算
- 下一轮在搬运

例如双缓冲：

```text
拍 0: copy g0 -> buf0
拍 1: gemm(buf0) + copy g1 -> buf1
拍 2: gemm(buf1) + copy g2 -> buf0
拍 3: gemm(buf0) + copy g3 -> buf1
```

这就是最典型的：

- 一边算当前块
- 一边准备下一块

## 17. 核间视角

同样的思想，也可以推广到核间。

也就是说，生产者和消费者不一定都在一个核里：

- 一个核负责生产中间结果
- 另一个核负责消费该结果

这时流水链不再只是：

```text
copy -> compute
```

而可能是：

```text
核 A 阶段 -> 核 B 阶段 -> 核 A 阶段 -> 核 B 阶段
```

或者其他更长的跨核链条。

所以从更一般的角度看：

- 核内流水和核间流水本质上都还是生产者-消费者流水
- 只是生产者和消费者属于不同硬件单元

## 18. GM 上的多缓冲

如果 GM 不只是原始输入存储，而还承担了：

- 中间结果暂存
- 跨核阶段交接
- 流水解耦层

那么 GM 本身也可以做多缓冲。

这时 GM 上多开几份 buffer 的意义是：

- 下游还在消费上一轮结果时
- 上游可以把下一轮结果写到另一个槽位

这样就不会因为同一块 GM 中间结果区域还被占着，而让上游完全停住。

所以：

- “GM 上做 multi-buffer”是有意义的
- 前提是 GM 在这个场景里承担了中间结果交接层的角色

## 19. 简化的 FA 风格跨核流水视角

如果用一个 NPU 上常见的、简化后的 FA 风格链路来抽象，可以写成：

- `c1`
- `v1`
- `c2`
- `v2`

其中：

- `c*` 可以理解成 cube 核上的矩阵型阶段
- `v*` 可以理解成 vector 核上的张量型阶段

那么一个逻辑迭代可以抽象为：

```text
c1 -> v1 -> c2 -> v2
```

如果不同逻辑迭代在这条链上错开流动，就形成跨核流水。

这里有个很重要的概念澄清：

- GM 的“版本 0 / 版本 1”更适合理解成“槽位 0 / 槽位 1”
- 而不是“整轮操作历史的整体副本”

也就是说：

- 某一类中间结果写入 GM 的 slot 0
- 下一轮同类中间结果写入 slot 1
- 再下一轮回到 slot 0

它更像是“buffer 的轮转”，而不是“整轮 c1,v1,c2,v2 全部打包成一个版本对象”。

## 20. 概念之间的关系总结

下面这几组关系最值得记住。

### 逻辑结构关系

- 循环次数决定有多少个逻辑迭代
- 每个逻辑迭代内部有自己的依赖结构
- 依赖结构决定一个逻辑迭代可以分成几个 stage
- `stage` 表示某个操作位于该逻辑迭代流水路径中的哪个位置

### 时间关系

- 拍 / 时间片是流水线推进的基本时间单位
- 一个拍里可以同时出现多个逻辑迭代的不同操作
- 流水深度表示同一时刻有多少轮逻辑迭代在流水线上活着

### 空间关系

- 多缓冲为这些活着的逻辑迭代提供独立存储槽位
- 流水越深，通常越需要更多 buffer 版本
- 如果 GM 也承担了中间交接层的角色，GM 也可以需要多缓冲

### 性能关系

- `num_stages` 表示允许的流水深度 / 轮转深度
- 更大的 `num_stages` 往往意味着更大的 buffer 需求
- 真正能否提升吞吐，还取决于硬件并发能力和依赖约束
- 更大的 `num_stages` 不自动等于更高真实加速

## 21. 最后给一个最实用的记忆方式

如果只保留一个最实用的理解框架，可以记成：

- `stage` 回答：一轮内部，这条操作处在流水线的哪个位置
- `拍` 回答：全局时间轴上，流水线现在推进到哪一步
- 流水深度回答：同一时刻，系统里活着多少轮逻辑迭代
- 多 buffer 数量回答：为了让这些活着的迭代共存不冲突，准备了多少个槽位

再压缩成一句话就是：

**时间上靠 stage 错开，空间上靠多缓冲分槽。**

这就是围绕 `T.Pipelined` 进行软件流水理解时最核心的思路。
